machine:
  ruby:
    version: 2.3.1
  node:
    version: 7.10.0
  post:
    - npm install -g npm@3
    - gem install slim sass

dependencies:
  override:
    - npm install -g gulp
    - npm install

test:
  override:
    - echo "skip all tests!"
  post:
    - gulp

deployment:
  master:
    branch: master
    commands:
      - sh script/deploy.sh