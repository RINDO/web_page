machine:
  ruby:
    version: 2.3.1

dependencies:
  override:
    - bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs=4 --retry=3

test:
  post:
    - bundle exec htmlproofer htdocs/ --disable-external --alt-ignore '/.*/' --file-ignore /.*vendor\/bundle*/

deployment:
  master:
    branch: master
    commands:
      - sh script/deploy.sh